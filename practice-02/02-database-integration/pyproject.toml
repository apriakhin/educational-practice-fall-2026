[project]
name = "partner-database-integration"
version = "0.1.0"
description = "Partner discount calculation with PostgreSQL integration"
requires-python = ">=3.9"
dependencies = [
    "psycopg2-binary==2.9.11",
]

[dependency-groups]
dev = [
    "pytest==8.3.5",
    "pytest-cov==6.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "--cov=app.discount --cov-report=term-missing --cov-fail-under=100"
